node_modules/
dist/
build/
.DS_Store
.vscode/
.idea/
*.log
*.tsbuildinfo

.pnpm-store/
.pnpm-debug.log*

DerivedData/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
*.xcuserstate
.swiftpm/
.build/
# NOTE: do NOT add `Packages/` here. On macOS's case-insensitive filesystem
# that pattern also swallows our `packages/` monorepo directory. Swift's
# `.build/checkouts/` already covers the only real use case.

vendor/
*.test
*.out
coverage.out

.env
.env.*
!.env.example
.dev.vars
.dev.vars.*

*.keychain
*.keychain-db
dev-signing-keychain-password
*_private.pem
*.p8
*.p12

.cache/
playwright-report/
test-results/
.turbo/
.next/
.vite/
.wrangler/
*.tsbuildinfo

packages/protocol/gen/
apps/signaling/internal/proto/
apps/mobile-pwa/src/lib/proto/

site/dist/
apps/mobile-pwa/dev-dist/

# Public Git stores sanitized evidence summaries only. Raw captures stay local.
docs/release-evidence/**/*.png
docs/release-evidence/**/*.jpg
docs/release-evidence/**/*.jpeg
docs/release-evidence/**/*.json
docs/release-evidence/**/*.html
docs/release-evidence/**/*.mov
docs/release-evidence/**/*.mp4
docs/release-evidence/**/*.webm
docs/release-evidence/**/*.trace
docs/release-evidence/**/*.zip
gitleaks-report.*
# Agent-factory runtime state must stay outside the public checkout. These rules
# are a final guard if an operator accidentally points the tools at the repo.
.gc/
.beads/
.factory-state/
