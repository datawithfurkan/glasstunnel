Privacy-reviewed summary artifact.
Date: 2026-09-02.
Commit: 2c4d343e.
Scope: Claude desktop app local contracts and store parsing: bundle id, claude:// scheme, desktop-owned transcripts, installed hooks, CLI flags, and the adapter's parse of real desktop sessions (title, model, turn state).
Result: pass for the scoped checks.
Evidence commands: pnpm qa:claude-desktop; GT_CLAUDE_REAL_STATE=1 swift test --package-path apps/host-macos --filter "testRealDesktopSessionsParse|testRealClaudeStoreOwnerAudit".
Observed: /Applications/Claude.app is com.anthropic.claudefordesktop and registers the claude:// scheme; 7 desktop-owned and 3 other sessions were classified correctly; the parser reported the running desktop session as working and finished sessions as ready, with their titles and models.
Also observed: every claude://code deep-link form (continue?session=…, needs-input, path form) is answered with "deep link gated off" in the app's own log on this build, which is why the adapter verifies the front window title and falls back to Accessibility.
Not covered: Accessibility prompt delivery and permission-dialog control on a real window (needs an Accessibility-trusted process), and the phone-driven flow through the mobile browser.
Raw logs, transcripts, and account details are intentionally not retained in Git.
