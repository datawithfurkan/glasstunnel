Privacy-reviewed summary artifact.
Date: 2026-09-02.
Commit: 95c792bf.
Scope: Claude desktop app local contracts, store parsing, and a read-only Accessibility probe: bundle id, claude:// scheme, running app, desktop-owned transcripts, installed hooks, CLI flags, the adapter's parse of real desktop sessions (title, model, turn state), and the composer/rename controls the app exposes.
Result: pass for the scoped checks.
Evidence commands: pnpm qa:claude-desktop:live-ax; GT_CLAUDE_REAL_STATE=1 swift test --package-path apps/host-macos --filter "testRealDesktopSessionsParse|testRealClaudeStoreOwnerAudit".
Observed: /Applications/Claude.app is com.anthropic.claudefordesktop, registers the claude:// scheme, and was running; 9 desktop-owned and 4 other sessions were classified correctly; the parser reported running sessions as working and finished sessions as ready, with their titles and models; the Glasstunnel hooks were installed; the composer is an editable text area described as "Prompt" and the session in front is named by a "<name>, rename session" control (the window title is always "Claude").
Also observed: every claude://code deep-link form (continue?session=…, needs-input, path form) is answered with "deep link gated off" in the app's own log on this build, which is why the adapter verifies the front session and falls back to Accessibility navigation.
Not covered: prompt delivery, session switching, permission-dialog control, and AskUserQuestion answers on a real window (pnpm lab:e2e:claude-desktop is ready and needs a dedicated app session with the window left alone).
Raw logs, transcripts, and account details are intentionally not retained in Git.
