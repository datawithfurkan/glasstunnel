Privacy-reviewed summary artifact.
Date: 2026-09-02.
Commit: 5c659eb6.
Scope: Claude desktop card driven from a phone-sized mobile Chromium through the Local Test Lab against the real Claude desktop app 1.40609.1, using a dedicated app session titled "Glasstunnel live evidence".
Result: pass.
Evidence command: pnpm lab:e2e:claude-desktop (Playwright project local-claude-desktop-mobile-chromium).
Card state trail recorded by the lab host: Opening on Mac → loading Claude context → switched the app to the dedicated session (sidebar press verified through the rename control) → prompt typed into the app's composer via Accessibility → Claude is working → Response ready (Stop hook) → shell-command prompt → Response ready (the session's "auto" permission mode approved the command itself; no dialog reached the phone) → AskUserQuestion prompt → Waiting for your answer (question published from the transcript) → answer sent (option pressed inside the session pane) → Response ready with the picked label in the reply. The Claude Code CLI card started alongside never showed Response ready or a decision.
Raw transcripts, session ids, account-specific output, and screenshots are intentionally not retained in Git.
